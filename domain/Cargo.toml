[package]
name = "domain"
version = "0.1.0"
edition = "2024"

[dependencies]
shared = { path = "../shared", version = "0.1.0" }
async-trait = "0.1.89"
chrono = { version = "0.4.42", default-features = false, features = ["clock"] }